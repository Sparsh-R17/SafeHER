enum Orientation {
  waiting,
  faceUp,
  faceDown,
  landscapeLeft,
  landscapeRight,
  portraitUp,
  portraitDown,
  error
}

enum Activity {
  downstairs,
  jogging,
  sitting,
  standing,
  upstairs,
  walking,
}

enum Reason {
  walking,
  travel,
  public,
  custom,
}

enum Auth {
  signup,
  login,
}
